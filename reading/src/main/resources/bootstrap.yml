spring:
  application:
    name: reading-service
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config-service
  profiles:
    active: development

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
